# Backend configuration (do not commit real secrets in .env)

# Optional: force a specific checkpoint file.
# Example: MODEL_CHECKPOINT=ml/runs/run_20260214_231102/best_model.pt
MODEL_CHECKPOINT=

# Optional: download checkpoint from a URL at startup.
# Example: MODEL_CHECKPOINT_URL=https://example.com/best_model.pt
MODEL_CHECKPOINT_URL=

# Optional: force a specific class-map file.
# Example: CLASS_MAP_PATH=data/processed/metadata/class_map.json
CLASS_MAP_PATH=

# Comma-separated list of allowed frontend origins for CORS.
# You can provide full URLs or bare hostnames.
# Example: CORS_ORIGINS=your-frontend-domain.com
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
